import 'package:flutter/material.dart';

class BalnceSheet extends StatelessWidget {
  const BalnceSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Balncesheet',style: TextStyle(
      color: Colors.amber,
    ),);
  }
}